import styled from '@emotion/styled';

const Chip = styled.span`
	background-color: var(--gray2);
	color: var(--gray3);
	border-radius: 15px;
	padding: 0.5rem 1rem;
	margin: 0.3rem;
`;

export default Chip;
